● 你说的两个 skill 是 project-wiki 和 human-insights，都在
  project-wiki/ 目录下。

  基于项目管理的视角，还可能有价值的 skill：

  ---
  高价值：

  1. task-spec — 从 backlog 取一个任务，帮你写成完整的
  spec（目标、边界、验收标准、依赖）。你已经有
  task-spec-user-onboard.md 这个实践，正好标准化
  2. code-review — 针对 vibe coding 场景的 code review，重点检查：AI
  生成代码的常见问题（过度工程化、边界条件缺失、安全漏洞）

  中等价值：

  3. retrospective —
  阶段结束后的复盘：交付了什么、踩了什么坑、下阶段要改进什么，产出写入
  wiki 和 human-insights
  4. bug-triage — 发现 bug 时的标准流程：复现、定位根因、检查类似
  bug（你刚在 CLAUDE.md 里加了这条）、写修复 spec

  ---
  我的判断：

  task-spec 最值得做，因为：
  - 你已经有了实践范本
  - 它在任务执行流程里是关键节点（roadmap/backlog → spec → review →
  执行）
  - 每个项目都需要，跨项目通用

  你觉得哪个最值得先做？


  ---
  retrospective — 写 skill 合理

  理由：
  - 执行逻辑复杂：读 backlog、读 changelog、读 pitfalls、对比
  roadmap、生成复盘报告、写入多个文件
  - 有明确的输入输出和产出格式
  - 触发时机特定（阶段结束），不是日常行为
  - 跨项目通用

  → 写 skill

  ---
  bug-triage — 写 CLAUDE.md 就够

  理由：
  - 本质是几条检查规则，不是复杂的多步骤流程
  - "发现 bug 时检查类似 bug" 这一条已经在 CLAUDE.md 里了
  - 补全成完整流程也就 4-5 步，不需要独立 skill 的触发机制
  - 过度 skill 化会让 CLAUDE.md 变薄、行为变得不透明

  → 补进 CLAUDE.md，就在你刚加的那行下面扩展成一个小节

  ┌────────┬──────────────────┬──────────────┐
  │        │      Skill       │  CLAUDE.md   │
  ├────────┼──────────────────┼──────────────┤
  │ 本质   │ 编排一套动作     │ 约束已有行为 │
  ├────────┼──────────────────┼──────────────┤
  │ 触发   │ 特定场景主动调用 │ 始终生效     │
  ├────────┼──────────────────┼──────────────┤
  │ 复杂度 │ 多步骤、跨文件   │ 几条规则     │
  └────────┴──────────────────┴──────────────┘