# Keep workspace dependencies nested under each package instead of hoisting
# into the repo root. This avoids Turbopack/module-resolution issues when
# building the app from source with npm.
install-strategy=nested

# Relax peer dependency enforcement for local development/builds.
strict-peer-deps=false
