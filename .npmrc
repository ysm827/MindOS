# Prevent npm from hoisting workspace dependencies to root.
# Without this, npm 11+ aggressively hoists packages like picocolors,
# source-map-js out of app/node_modules/ into root node_modules/,
# which breaks Turbopack's module resolution during `next build`.
hoist=false
